# gradle.properties
org.gradle.caching=true
org.gradle.parallel=true

# Allow Gradle to auto-provision the Java 25 toolchain when it is missing locally.
org.gradle.java.installations.auto-download=true
